name: Db Scheduler
description: Persistent cluster-friendly scheduler using a single database table, integrating db-scheduler with quarkus-scheduler.
metadata:
  keywords:
    - db-scheduler
    - scheduler
    - persistent
    - cluster
  categories:
    - "miscellaneous"
  lib-ga: com.github.kagkarlsson:db-scheduler[https://github.com/kagkarlsson/db-scheduler]
  status: "preview"
